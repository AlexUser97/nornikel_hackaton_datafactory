"""Общие настройки и константы пакета.

Держим всё в одном месте, чтобы UI, расчёты и PDF не расходились по именам фаз
и цветам оверлея.
"""
from __future__ import annotations

# Имена фаз по умолчанию (fallback-путь и синтетическое демо на 3 фазы по яркости).
# Нейтральные имена — реальный профиль аншлифа руды (5 классов) всегда передаётся явно
# через MATERIAL_PROFILES / soft_segment_fn.
DEFAULT_CLASS_NAMES: list[str] = ["тёмная фаза", "серая фаза", "светлая фаза"]

# Порог талька для классификации руды (официальное ТЗ task-3): >10% → оталькованная.
TALC_THRESHOLD: float = 0.10

# Цвета классов задачи ТЗ (по индексу): 0=тонкие(красный), 1=обычные(зелёный),
# 2=тальк(синий), 3=вмещающая порода(серый).
ORE_PETRO_COLORS: list[tuple[int, int, int]] = [
    (214, 39, 40), (46, 204, 113), (52, 120, 219), (120, 128, 140)]

# Число классов сегментации. Фиксировано — в UI менять нельзя.
ORE_N_CLASSES: int = 4

# Единственный профиль материала по актуальному ТЗ — аншлиф сульфидной руды Ni-Cu
# (оптическая микроскопия). 4 класса: тонкие/обычные срастания, тальк (зоны оталькования),
# вмещающая порода. Сульфиды/магнетит — обученной моделью (LumenStone S2 + доменная
# адаптация), тальк — U-Net на клин-твинах «Области оталькования».
MATERIAL_PROFILES: dict[str, dict] = {
    "Аншлиф руды (оптическая микроскопия)": {
        "class_names": ["тонкие срастания", "обычные срастания", "тальк", "вмещающая порода"],
        "formulas": ["", "", "", ""],
        "colors": ORE_PETRO_COLORS,
        "astm_applicable": False,
        "classify": "ore_intergrowth",
        "segmenter": "ore_lumen",  # LumenStone-сегментатор (shlif.ore_lumen), фолбэк — классика
        "size_label": "Средний размер рудных вкрапленников",
        "method": ("Панорамная оптическая микроскопия аншлифа; сегментация сульфидных срастаний "
                   "(обычные/тонкие), талька и вмещающей породы, классификация сорта руды"),
    },
}

# Максимальное число фаз (для этой задачи фиксировано 4, см. ORE_N_CLASSES).
MAX_CLASSES: int = 4

# Подсказки к неочевидным для инженера полям (тултипы в UI).
METRIC_HELP: dict[str, str] = {
    "grain_size": "Средний эквивалентный диаметр зерна (по площади). Не максимум — среднее по всем "
                  "учтённым зёрнам; ± — стандартное отклонение распределения.",
    "grain_count": "Сколько отдельных зёрен выделено и учтено в статистике размера "
                   "(пограничные, обрезанные рамкой кадра, исключены).",
    "astm": "Балл размера зерна по ASTM E112 (для сталей/сплавов). Больше балл — мельче зерно.",
    "defects": "Число обнаруженных пор/включений (тёмные округлые объекты). Это НЕ «не определено».",
    "undetermined": "Доля площади, где модель не уверена в фазе (уверенность ниже порога). "
                    "Это индикатор неопределённости, а не отдельная фаза и не дефект.",
    "runs": "Сколько раз повторяется анализ с лёгкими искажениями входа для оценки неопределённости. "
            "Больше повторов — точнее оценка, но дольше счёт. Инженеру менять не обязательно.",
    "confidence": "Общая уверенность = 1 − средняя неопределённость по кадру.",
}

# Описание используемых методов/моделей — для раздела «Помощь».
METHODS_HELP: str = (
    "**Сегментация (аншлиф руды) — 4 класса по ТЗ.** Сульфидные срастания делятся на обычные "
    "(крупные, слабо замещённые — зелёный) и тонкие (сильно замещённые магнетитом/серой фазой — "
    "красный). Тальк (синий) — зоны оталькования (тёмные вкрапления в матрице). Вмещающая порода "
    "(серый) — прочая нерудная матрица. Сульфиды/магнетит — обученной моделью на внешнем датасете "
    "**LumenStone S2** (МГУ, Cu-Ni руды, реальный пиксельный GT) + доменная адаптация (Reinhard); "
    "тальк — U-Net на клин-твинах «Области оталькования».\n\n"
    "**Классификация сорта.** Обученный EfficientNet-B0 (open-license, ImageNet + сильная color-adaptation "
    "аугментация): рядовая / труднообогатимая / оталькованная.\n\n"
    "**Доли площадей.** Общая доля сульфидов, доля талька, по типам срастаний, вмещающая порода.\n\n"
    "**Оценка неопределённости.** Ансамбль лёгких искажений входа → карта спорных зон для эксперта.\n\n"
    "**Метрики качества.** IoU и расстояние Хаусдорфа (сегментация), F1 и AUC (классификация)."
)

# Профиль по умолчанию — задача официального ТЗ (аншлиф руды, опт. микроскопия).
DEFAULT_PROFILE: str = "Аншлиф руды (оптическая микроскопия)"

# Реквизиты лаборатории для шапки протокола испытаний (ГОСТ ISO/IEC 17025-2019,
# ГОСТ Р 58973-2020). Значения-заглушки: заполняются аккредитованной лабораторией.
LAB_DEFAULTS: dict[str, str] = {
    "lab_name": "Испытательный аналитический центр (указать лабораторию)",
    "lab_address": "Адрес лаборатории (указать)",
    "accreditation": "Аттестат аккредитации (указать №); ГОСТ ISO/IEC 17025-2019",
}

# Дисклеймер статуса метода — обязателен в документе (отчёт v2, §7.5, §8).
METHOD_DISCLAIMER: str = (
    "Результат получен неаттестованной методикой автоматической сегментации по оптическому "
    "изображению и является средством поддержки эксперта и первичной оценкой, а не аттестованным "
    "измерением фазового состава. Спорные зоны и оценка неопределённости требуют верификации "
    "экспертом. Документ приобретает юридическую значимость после утверждения аккредитованной "
    "лабораторией (присвоение номера и подпись уполномоченного лица)."
)

# Пороги для сводной оценки «уверенности» результата (отчёт v2, §6.1).
# Зелёный статус — только если одновременно: общая уверенность высока И мало «не определено»
# И нет спорных зон. Иначе — предупреждение о необходимости верификации.
OVERALL_CONF_OK: float = 0.85       # порог общей уверенности
UNDETERMINED_OK: float = 0.05       # порог доли «не определено»

# Палитра оверлея (RGB, 0..255) по индексу класса (тёмная фаза -> светлая).
# Согласована с макетом UI: перлит=фиолетовый, бейнит=оранжевый, феррит=зелёный.
PHASE_COLORS: list[tuple[int, int, int]] = [
    (139, 92, 246),   # 0 — фиолетовый (перлит)
    (230, 126, 34),   # 1 — оранжевый (бейнит)
    (46, 204, 113),   # 2 — зелёный (феррит)
    (52, 152, 219),   # 3 — синий
    (241, 196, 15),   # 4 — жёлтый
    (231, 76, 60),    # 5 — красный
]

# Число прогонов ансамбля для оценки неуверенности (разброс по пертурбациям).
# Больше -> честнее оценка, но дольше. 5 — компромисс под интерактив.
ENSEMBLE_RUNS: int = 5

# Порог уверенности: пиксели с confidence ниже уходят в «зоны на проверку».
LOW_CONF_THRESHOLD: float = 0.60

# Минимальная площадь зоны низкой уверенности (в пикселях), чтобы не сыпать шумом.
MIN_LOW_CONF_AREA: int = 400

# Масштаб по умолчанию, если пользователь не задал (µm на пиксель).
DEFAULT_SCALE_UM_PER_PX: float = 0.5
